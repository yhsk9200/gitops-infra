apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: postgres-db-secret
  namespace: default
spec:
  encryptedData:
    postgres-password: test
    keycloak-password: test
  template:
    metadata:
      name: postgres-db-secret
      namespace: default
    type: Opaque
