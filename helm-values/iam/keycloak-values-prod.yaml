# =============================================================================
# Keycloak values for production exposure (aporiax-auth.duckdns.org)
# =============================================================================
# Active since 2026-07-03: domain/TLS confirmed (DuckDNS + letsencrypt-prod).
# TLS terminates at Traefik; Keycloak serves HTTP internally behind
# proxyHeaders=xforwarded, so httpEnabled stays true.
# =============================================================================

global:
  security:
    allowInsecureImages: true

image:
  registry: docker.io
  repository: bitnamilegacy/keycloak
  tag: 26.3.3-debian-12-r0

auth:
  adminUser: admin
  existingSecret: keycloak-admin-secret
  passwordSecretKey: admin-password

postgresql:
  enabled: false

externalDatabase:
  host: platform-db-postgres-postgresql.platform-db.svc.cluster.local
  port: 5432
  user: keycloak_admin
  database: keycloak_db
  existingSecret: keycloak-db-secret
  existingSecretPasswordKey: keycloak-password

production: true
proxyHeaders: xforwarded
hostnameStrict: true
httpEnabled: true

ingress:
  enabled: true
  ingressClassName: traefik
  hostname: aporiax-auth.duckdns.org
  tls: true
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    cert-manager.io/cluster-issuer: letsencrypt-prod

automountServiceAccountToken: false

serviceAccount:
  create: true
  automountServiceAccountToken: false

replicaCount: 1

resources:
  requests:
    cpu: 500m
    memory: 1Gi
  limits:
    cpu: "2"
    memory: 2Gi

logging:
  output: default
  level: INFO

metrics:
  enabled: false
  serviceMonitor:
    enabled: false

# Enable only after the realm/client/role JSON files are reviewed.
keycloakConfigCli:
  enabled: false

# Start permissive and tighten after CNI support and traffic paths are verified.
networkPolicy:
  enabled: true
  allowExternal: true
  allowExternalEgress: true
